import commonStyles from './common';

const dark = {
    ...commonStyles,
    backgroundColor: '#222',
    textColor: '#fff',
    headerBackgroundColor: '#111',
    footerBackgroundColor: '#111',
    genreTagColor: 'rgba(0, 0, 0, 0.3)'
}

export default dark;