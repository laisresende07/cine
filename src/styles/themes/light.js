import commonStyles from './common';

const light = {
    ...commonStyles,
    backgroundColor: '#ddd',
    textColor: '#222',
    headerBackgroundColor: '#fff',
    footerBackgroundColor: '#fff',
    genreTagColor: 'rgba(255, 255, 255, 0.3)'
}

export default light